ostream>;
#include <math.h>;
using namespace std;
float S, a, b, c, d, e, r;

void triangle(a, b, c) {
	cout << "Введите три стороны треугольника" << "\n";
	float a, b, c;
	cin >> a >> b >> c;
	float p = a + b + c;
	S = sqrt(p * (p - a) * (p - b) * (p - c));
	cout << "Площадь заявленного треугольника - " << S << "\n";
}
void rectangle(d, e) {
	S = d * e;
	cout << "Площадь заявленного прямоугольника - " << S << "\n";
}
void circle(r) {
	float r;
	cout << "Введите радиус круга" << "\n";
	float r;
	cin >> r;
	S = 3, 14 * pow(r, 2);
	cout << "Площадь заявленного круга - " << S << "\n";
}



int main() {
	float D;
	setlocale(LC_ALL, "Russian");
	cout << "Введите фигуру: 1 - если прямоугольник, 2 - если треугольник, 3 - если круг" << '\n';
	int name;
	cin >> name;
	switch (name) {
		case "Прямоугольник:
			cout << "Введите стороны прямоугольника" << "\n";
			cin >> d >> e;
			rectangle(d, e);*/
